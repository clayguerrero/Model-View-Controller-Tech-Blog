<div class="postsContainer">
  <h2 class="yourPosts">YOUR POSTS</h2>
  {{#each posts as |post|}}
    <div id="card{{this.id}}" class="post">
      <a href="/post/{{this.id}}">{{this.title}} </a>
      <p>{{this.post}}</p>
    </div>
  {{/each}}
</div>
